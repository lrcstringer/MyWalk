// Smoke test — verifies the app compiles and TributeApp can be referenced.
// Full integration tests are deferred until Firebase Test Lab is configured.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
